/*
Name: Leanza Armamento 

libraries
*/
#include <iostream>
#include <cmath>
#include <cstdlib>
// #include <cstdio>
#include <cassert>
#include <string>
#include <random>
#include <cctype>
#include <iomanip>
// #include <cstido>

using namespace std;

int main()
{
    cout << boolalpha;
    cout << (1 == 1) << endl;
    cout << noboolalpha
    cout << boolalpha << (1 != 1) << endl;
    cout << boolalpha << (1 < 1) << endl; 
    cout << boolalpha << (1 <= 1) << endl;
    cout << boolalpha << (1 > 1) << endl; 
    cout << boolalpha << (1 >= 1) << endl; 


    //int nameW, titleW, positionW

    //nameW = 17;
    //titleW = 25;
    //positionW = 18;

    //cout << left;
    //cout << setfill('-');
    //cout << setw(nameW) << "Name" << setw(titleW) << "Title" << setw(postionW) << "Position" << endl;
    // instead of this use below cout << "=========================================" << endl;

    //cout << setfill ('=');
    //cout << setw(nameW+titleW+positionW) << "" << endl;
    //cout << setfill (' '); 

    //cout <<setw(nameW) << "Leanza" << setw(titleW) << "Mooshu" << setw(positionW) << "All seeing"
    //cout << setw(nameW) << "Steve" << setw (titleW) << " Court Jester" << set(positionW) << "Fool" << endl;
    //cout << setw(nameW) << "Doofenschmirtz" << setw(titleW) << "Accountant" << set(positionW) << "Head Accountant" << endl;
    //cout << right;

    //cout.setf(ios::fixed, ios::floatfield);
    //cout << setprecision(4) << 42.123123123 << endl;
    // cout << "\tName" << "\tTitle" << "\tPosition" << endl; 

    //cout << "Please press enter to quit." << endl;
    // cin.get();
    // cin.ignore (1000, '\n');
    //getchar();


    // cout << setw(10) << "Name" << setw(10) << "Title" << setw(10) << "Position" << endl;
    // cout << "\tName" << "\tTitle" << "\tPosition" << endl;

    //cout << setw(10) << "HelloWorld!!!!" << endl;
    //char someCh = 'a';

    //char someCh = 'A';
    //cout << (char)tolower(someCh) << endl;

    //srand(time(0));
    //cout << (rand()%100)+1 << endl;

    //string phrase;

    //phrase = "This is a sentence. ";

    //cout << "The length of our phrase is: " << phrase.length() << endl;

    //phrase.append(" And this is anotehr sentence.");

    //cout << "The length of our phrase is: " << phrase.length() << endl;

    //phrase
    //float n1, n2;
    //float sum;

    //cout << "Enter number 1: ";
    //cin >> n1;

    //cout << "Enter number 2: ";
    //cin >> n2;

    //sum = n1 +n2;

    //assert(sum == 57)

    //cout << n1 << "+" << n2 << "=" << sum << endl;
    //printf("%d + %d = %d\n", n1, n2, sum); 

    //double someNumber;

    //someNumber = atof("42.a23, 2");
    //cout << someNumber << endl;
    //cout << (123%2 == 1) << endl;
    //int numSeconds;
    //int origSeconds; 
    //int numMinutes;
    //int numHours;

    //cout << "Enter number of seconds"
    //cin >> numSeconds;
    //origSeconds - numSeconds; 

    //numHours = numSeconds/3600;
    //numMinutes = (numSeconds/60) - (numHours *60);
    //numSeconds = numSeconds%60; 

    //cout << "The hours:minutes:seconds given" << origSeconds << "seconds is:"
        //<< numHours << ":" << numMinutes << ":" << numSeconds << endl;
    //numMinutes = (numMinutes - (numHours * 60)) % 60; 

    //cout << 127%60 << endl;
    //cout << 42 / (15 * 8) << endl;
    // cout << (180/3.14)*cos(42) << endl;
    // float result = ((180/3.14)*cos(42))+360;
    // cout << result << endl;
    // cout << (180/M_SQRT1_2)*cos(42) << endl;
    // int n1, n2;
    // n1 = 2;
    // n2 = 8;
    // cout << (n1^n2) << endl;
    return 0;
}